diff --git a/dlls/ntdll/tests/sync.c b/dlls/ntdll/tests/sync.c
index b7c278f00ab..6688b126fc3 100644
--- a/dlls/ntdll/tests/sync.c
+++ b/dlls/ntdll/tests/sync.c
@@ -141,9 +141,20 @@ static void test_event(void)
     ok( status == STATUS_SUCCESS, "NtSetEvent failed: %08lx\n", status );
     ok( !prev_state, "prev_state = %lx\n", prev_state );
 
-    status = pNtPulseEvent( event, &prev_state );
-    ok( status == STATUS_SUCCESS, "NtPulseEvent failed %08lx\n", status );
-    ok( prev_state == 1, "prev_state = %lx\n", prev_state );
+    /* Test access mask validation */
+    status = pNtCreateEvent(&event2, SYNCHRONIZE, &attr, NotificationEvent, 0);
+    ok( status == STATUS_SUCCESS, "NtCreateEvent with SYNCHRONIZE failed %08lx\n", status );
+
+    status = pNtSetEvent( event2, NULL );
+    ok( status == STATUS_ACCESS_DENIED, "NtSetEvent without EVENT_MODIFY_STATE expected STATUS_ACCESS_DENIED, got %08lx\n", status );
+
+    status = pNtResetEvent( event2, NULL );
+    ok( status == STATUS_ACCESS_DENIED, "NtResetEvent without EVENT_MODIFY_STATE expected STATUS_ACCESS_DENIED, got %08lx\n", status );
+
+    status = pNtPulseEvent( event2, NULL );
+    ok( status == STATUS_ACCESS_DENIED, "NtPulseEvent without EVENT_MODIFY_STATE expected STATUS_ACCESS_DENIED, got %08lx\n", status );
+
+    pNtClose(event2);
 
     pNtClose(event);
 }
@@ -525,7 +536,17 @@ static void test_semaphore(void)
     status = pNtQuerySemaphore(semaphore, SemaphoreBasicInformation, &info, sizeof(info), NULL);
     ok( status == STATUS_SUCCESS, "NtQuerySemaphore failed %08lx\n", status );
     ok( info.CurrentCount == 2, "expected 2, got %ld\n", info.CurrentCount );
-    ok( info.MaximumCount == 2, "expected 2, got %ld\n", info.MaximumCount );
+    /* Test access mask validation */
+    {
+        HANDLE sem2;
+        status = pNtCreateSemaphore(&sem2, SYNCHRONIZE, &attr, 1, 2);
+        ok( status == STATUS_SUCCESS, "pNtCreateSemaphore with SYNCHRONIZE failed %08lx\n", status );
+
+        status = pNtReleaseSemaphore(sem2, 1, NULL);
+        ok( status == STATUS_ACCESS_DENIED, "NtReleaseSemaphore without SEMAPHORE_MODIFY_STATE expected STATUS_ACCESS_DENIED, got %08lx\n", status );
+
+        NtClose( sem2 );
+    }
 
     NtClose( semaphore );
 }
diff --git a/dlls/ntdll/unix/esync.c b/dlls/ntdll/unix/esync.c
index 02a93f134ad..6791b3610ce 100644
--- a/dlls/ntdll/unix/esync.c
+++ b/dlls/ntdll/unix/esync.c
@@ -76,6 +76,7 @@ struct esync
     LONG type;
     int fd;
     void *shm;
+    ACCESS_MASK access;
 };
 
 struct semaphore
@@ -111,68 +112,45 @@ static pthread_mutex_t shm_addrs_mutex = PTHREAD_MUTEX_INITIALIZER;
 #include <alloca.h>
 #include <errno.h>
 static int shm_unlink(const char *name) {
-    size_t namelen;
     const char *env_tmpdir = getenv("TMPDIR");
     const char *default_tmpdir = "/data/data/com.windroid.emu/files/usr/tmp/";
-
+    const char *dir = env_tmpdir ? env_tmpdir : default_tmpdir;
     char fname[1024];
+    size_t pathlen;
 
-    if (env_tmpdir) {
-        size_t pathlen = strlen(env_tmpdir);
-        snprintf(fname, sizeof(fname), "%s%s", env_tmpdir, (env_tmpdir[pathlen - 1] == '/') ? "" : "/");
-    } else {
-        snprintf(fname, sizeof(fname), "%s", default_tmpdir);
-    }
-
-    /* Construct the filename.  */
     while (name[0] == '/') ++name;
-
     if (name[0] == '\0') {
-        /* The name "/" is not supported.  */
         errno = EINVAL;
         return -1;
     }
 
-    strcat(fname, name);
-
+    pathlen = strlen(dir);
+    snprintf(fname, sizeof(fname), "%s%s%s", dir, (pathlen > 0 && dir[pathlen - 1] == '/') ? "" : "/", name);
     return unlink(fname);
 }
 
 static int shm_open(const char *name, int oflag, mode_t mode) {
-    size_t namelen;
-    int fd;
     const char *env_tmpdir = getenv("TMPDIR");
     const char *default_tmpdir = "/data/data/com.windroid.emu/files/usr/tmp/";
-
+    const char *dir = env_tmpdir ? env_tmpdir : default_tmpdir;
     char fname[1024];
+    size_t pathlen;
+    int fd;
 
-    if (env_tmpdir) {
-        size_t pathlen = strlen(env_tmpdir);
-        snprintf(fname, sizeof(fname), "%s%s", env_tmpdir, (env_tmpdir[pathlen - 1] == '/') ? "" : "/");
-    } else {
-        snprintf(fname, sizeof(fname), "%s", default_tmpdir);
-    }
-
-    /* Construct the filename.  */
     while (name[0] == '/') ++name;
-
     if (name[0] == '\0') {
-        /* The name "/" is not supported.  */
         errno = EINVAL;
         return -1;
     }
 
-    strcat(fname, name);
+    pathlen = strlen(dir);
+    snprintf(fname, sizeof(fname), "%s%s%s", dir, (pathlen > 0 && dir[pathlen - 1] == '/') ? "" : "/", name);
 
     fd = open(fname, oflag, mode);
     if (fd != -1) {
-        /* We got a descriptor.  Now set the FD_CLOEXEC bit.  */
         int flags = fcntl(fd, F_GETFD, 0);
         flags |= FD_CLOEXEC;
-        flags = fcntl(fd, F_SETFD, flags);
-
-        if (flags == -1) {
-            /* Something went wrong.  We cannot return the descriptor.  */
+        if (fcntl(fd, F_SETFD, flags) == -1) {
             int save_errno = errno;
             close(fd);
             fd = -1;
@@ -237,7 +215,7 @@ static inline UINT_PTR handle_to_index( HANDLE handle, UINT_PTR *entry )
     return idx % ESYNC_LIST_BLOCK_SIZE;
 }
 
-static struct esync *add_to_list( HANDLE handle, enum esync_type type, int fd, void *shm )
+static struct esync *add_to_list( HANDLE handle, enum esync_type type, int fd, void *shm, ACCESS_MASK access )
 {
     UINT_PTR entry, idx = handle_to_index( handle, &entry );
 
@@ -263,6 +241,7 @@ static struct esync *add_to_list( HANDLE handle, enum esync_type type, int fd, v
     {
         esync_list[entry][idx].fd = fd;
         esync_list[entry][idx].shm = shm;
+        esync_list[entry][idx].access = access;
     }
     return &esync_list[entry][idx];
 }
@@ -286,6 +265,7 @@ static NTSTATUS get_object( HANDLE handle, struct esync **obj )
     int ret = STATUS_SUCCESS;
     enum esync_type type = 0;
     unsigned int shm_idx = 0;
+    ACCESS_MASK access = 0;
     obj_handle_t fd_handle;
     sigset_t sigset;
     int fd = -1;
@@ -294,7 +274,72 @@ static NTSTATUS get_object( HANDLE handle, struct esync **obj )
 
     if ((INT_PTR)handle < 0)
     {
-        /* We can deal with pseudo-handles, but it's just easier this way */
+        if (handle == (HANDLE)-1 || handle == (HANDLE)-2)
+        {
+            static struct esync process_obj;
+            static int process_obj_init;
+            static __thread struct esync thread_obj;
+            static __thread int thread_obj_init;
+
+            if (handle == (HANDLE)-1)
+            {
+                if (!process_obj_init)
+                {
+                    server_enter_uninterrupted_section( &fd_cache_mutex, &sigset );
+                    if (!process_obj_init)
+                    {
+                        SERVER_START_REQ( get_esync_fd )
+                        {
+                            req->handle = wine_server_obj_handle( handle );
+                            if (!(ret = wine_server_call( req )))
+                            {
+                                process_obj.type = reply->type;
+                                process_obj.access = reply->access;
+                                process_obj.fd = receive_fd( &fd_handle );
+                                process_obj.shm = NULL;
+                                process_obj_init = 1;
+                            }
+                        }
+                        SERVER_END_REQ;
+                    }
+                    server_leave_uninterrupted_section( &fd_cache_mutex, &sigset );
+                }
+                if (process_obj_init)
+                {
+                    *obj = &process_obj;
+                    return STATUS_SUCCESS;
+                }
+            }
+            else if (handle == (HANDLE)-2)
+            {
+                if (!thread_obj_init)
+                {
+                    server_enter_uninterrupted_section( &fd_cache_mutex, &sigset );
+                    if (!thread_obj_init)
+                    {
+                        SERVER_START_REQ( get_esync_fd )
+                        {
+                            req->handle = wine_server_obj_handle( handle );
+                            if (!(ret = wine_server_call( req )))
+                            {
+                                thread_obj.type = reply->type;
+                                thread_obj.access = reply->access;
+                                thread_obj.fd = receive_fd( &fd_handle );
+                                thread_obj.shm = NULL;
+                                thread_obj_init = 1;
+                            }
+                        }
+                        SERVER_END_REQ;
+                    }
+                    server_leave_uninterrupted_section( &fd_cache_mutex, &sigset );
+                }
+                if (thread_obj_init)
+                {
+                    *obj = &thread_obj;
+                    return STATUS_SUCCESS;
+                }
+            }
+        }
         return STATUS_NOT_IMPLEMENTED;
     }
 
@@ -316,6 +361,7 @@ static NTSTATUS get_object( HANDLE handle, struct esync **obj )
             {
                 type = reply->type;
                 shm_idx = reply->shm_idx;
+                access = reply->access;
                 fd = receive_fd( &fd_handle );
                 assert( wine_server_ptr_handle(fd_handle) == handle );
             }
@@ -339,7 +385,7 @@ static NTSTATUS get_object( HANDLE handle, struct esync **obj )
 
     TRACE("Got fd %d for handle %p.\n", fd, handle);
 
-    *obj = add_to_list( handle, type, fd, shm_idx ? get_shm( shm_idx ) : 0 );
+    *obj = add_to_list( handle, type, fd, shm_idx ? get_shm( shm_idx ) : 0, access );
     return ret;
 }
 
@@ -399,7 +445,7 @@ static NTSTATUS create_esync( enum esync_type type, HANDLE *handle, ACCESS_MASK
 
     if (!ret || ret == STATUS_OBJECT_NAME_EXISTS)
     {
-        add_to_list( *handle, type, fd, shm_idx ? get_shm( shm_idx ) : 0 );
+        add_to_list( *handle, type, fd, shm_idx ? get_shm( shm_idx ) : 0, access );
         TRACE("-> handle %p, fd %d.\n", *handle, fd);
     }
 
@@ -439,7 +485,7 @@ static NTSTATUS open_esync( enum esync_type type, HANDLE *handle,
 
     if (!ret)
     {
-        add_to_list( *handle, type, fd, shm_idx ? get_shm( shm_idx ) : 0 );
+        add_to_list( *handle, type, fd, shm_idx ? get_shm( shm_idx ) : 0, access );
 
         TRACE("-> handle %p, fd %d.\n", *handle, fd);
     }
@@ -474,6 +520,7 @@ NTSTATUS esync_release_semaphore( HANDLE handle, unsigned int count, ULONG *prev
     TRACE("%p, %d, %p.\n", handle, count, prev);
 
     if ((ret = get_object( handle, &obj))) return ret;
+    if (!(obj->access & SEMAPHORE_MODIFY_STATE)) return STATUS_ACCESS_DENIED;
     semaphore = obj->shm;
 
     do
@@ -603,6 +650,7 @@ NTSTATUS esync_set_event( HANDLE handle )
     TRACE("%p.\n", handle);
 
     if ((ret = get_object( handle, &obj ))) return ret;
+    if (!(obj->access & EVENT_MODIFY_STATE)) return STATUS_ACCESS_DENIED;
     event = obj->shm;
 
     if (obj->type == ESYNC_MANUAL_EVENT)
@@ -646,6 +694,7 @@ NTSTATUS esync_reset_event( HANDLE handle )
     TRACE("%p.\n", handle);
 
     if ((ret = get_object( handle, &obj ))) return ret;
+    if (!(obj->access & EVENT_MODIFY_STATE)) return STATUS_ACCESS_DENIED;
     event = obj->shm;
 
     if (obj->type == ESYNC_MANUAL_EVENT)
@@ -682,23 +731,58 @@ NTSTATUS esync_pulse_event( HANDLE handle )
 {
     uint64_t value = 1;
     struct esync *obj;
+    struct event *event;
     NTSTATUS ret;
 
     TRACE("%p.\n", handle);
 
     if ((ret = get_object( handle, &obj ))) return ret;
+    if (!(obj->access & EVENT_MODIFY_STATE)) return STATUS_ACCESS_DENIED;
 
-    /* This isn't really correct; an application could miss the write.
-     * Unfortunately we can't really do much better. Fortunately this is rarely
-     * used (and publicly deprecated). */
-    if (write( obj->fd, &value, sizeof(value) ) == -1)
-        return errno_to_status( errno );
+    event = obj->shm;
+
+    if (obj->type == ESYNC_MANUAL_EVENT)
+    {
+        /* Acquire the spinlock. */
+        while (InterlockedCompareExchange( &event->locked, 1, 0 ))
+            small_pause();
 
-    /* Try to give other threads a chance to wake up. Hopefully erring on this
-     * side is the better thing to do... */
-    NtYieldExecution();
+        InterlockedExchange( &event->signaled, 1 );
 
-    read( obj->fd, &value, sizeof(value) );
+        if (write( obj->fd, &value, sizeof(value) ) == -1)
+            ERR("write: %s\n", strerror(errno));
+
+        event->locked = 0;
+
+        /* Yield execution to give waiting threads a chance to wake up. */
+        NtYieldExecution();
+
+        while (InterlockedCompareExchange( &event->locked, 1, 0 ))
+            small_pause();
+
+        InterlockedExchange( &event->signaled, 0 );
+        if (read( obj->fd, &value, sizeof(value) ) == -1 && errno != EWOULDBLOCK && errno != EAGAIN)
+        {
+            ERR("read: %s\n", strerror(errno));
+        }
+
+        event->locked = 0;
+    }
+    else
+    {
+        InterlockedExchange( &event->signaled, 1 );
+
+        if (write( obj->fd, &value, sizeof(value) ) == -1)
+            ERR("write: %s\n", strerror(errno));
+
+        NtYieldExecution();
+
+        InterlockedExchange( &event->signaled, 0 );
+        if (read( obj->fd, &value, sizeof(value) ) == -1 && errno != EWOULDBLOCK && errno != EAGAIN)
+        {
+            ERR("read: %s\n", strerror(errno));
+        }
+    }
 
     return STATUS_SUCCESS;
 }
@@ -750,6 +834,7 @@ NTSTATUS esync_release_mutex( HANDLE *handle, LONG *prev )
     TRACE("%p, %p.\n", handle, prev);
 
     if ((ret = get_object( handle, &obj ))) return ret;
+    if (!(obj->access & MUTANT_QUERY_STATE)) return STATUS_ACCESS_DENIED;
     mutex = obj->shm;
 
     /* This is thread-safe, because the only thread that can change the tid to
@@ -931,7 +1016,10 @@ static NTSTATUS __esync_wait_objects( unsigned int count, const HANDLE *handles,
     {
         ret = get_object( handles[i], &objs[i] );
         if (ret == STATUS_SUCCESS)
+        {
+            if (!(objs[i]->access & SYNCHRONIZE)) return STATUS_ACCESS_DENIED;
             has_esync = 1;
+        }
         else if (ret == STATUS_NOT_IMPLEMENTED)
             has_server = 1;
         else
@@ -941,10 +1029,10 @@ static NTSTATUS __esync_wait_objects( unsigned int count, const HANDLE *handles,
     if (objs[count - 1] && objs[count - 1]->type == ESYNC_QUEUE)
         msgwait = TRUE;
 
-    if (has_esync && has_server)
-        FIXME("Can't wait on esync and server objects at the same time!\n");
-    else if (has_server)
+    if (has_server && !has_esync)
         return STATUS_NOT_IMPLEMENTED;
+    else if (has_esync && has_server)
+        TRACE("Doing mixed wait (esync + server objects) for %d handles.\n", count);
 
     if (TRACE_ON(esync))
     {
@@ -1070,7 +1158,50 @@ static NTSTATUS __esync_wait_objects( unsigned int count, const HANDLE *handles,
 
         while (1)
         {
-            ret = do_poll( fds, pollcount, timeout ? &end : NULL );
+            ULONGLONG poll_end = 0;
+            if (has_server)
+            {
+                union select_op select_op;
+                LARGE_INTEGER zero = {0};
+                UINT server_count = 0;
+                int server_indices[MAXIMUM_WAIT_OBJECTS];
+                UINT flags = SELECT_INTERRUPTIBLE;
+
+                if (alertable) flags |= SELECT_ALERTABLE;
+                select_op.wait.op = SELECT_WAIT;
+                for (i = 0; i < count; i++)
+                {
+                    if (!objs[i])
+                    {
+                        server_indices[server_count] = i;
+                        select_op.wait.handles[server_count] = wine_server_obj_handle( handles[i] );
+                        server_count++;
+                    }
+                }
+                if (server_count)
+                {
+                    int res = server_wait( &select_op, offsetof( union select_op, wait.handles[server_count] ),
+                                          flags, &zero );
+                    if (res >= 0 && res < (int)server_count)
+                    {
+                        TRACE("Woken up by server handle %p [%d].\n", handles[server_indices[res]], server_indices[res]);
+                        return server_indices[res];
+                    }
+                    else if (res >= STATUS_ABANDONED_WAIT_0 && res < STATUS_ABANDONED_WAIT_0 + (int)server_count)
+                    {
+                        return STATUS_ABANDONED_WAIT_0 + server_indices[res - STATUS_ABANDONED_WAIT_0];
+                    }
+                    else if (res == STATUS_USER_APC)
+                    {
+                        goto userapc;
+                    }
+                }
+                NtQuerySystemTime( &now );
+                poll_end = now.QuadPart + 50000; /* 5ms step for server check */
+                if (timeout && end < poll_end) poll_end = end;
+            }
+
+            ret = do_poll( fds, pollcount, has_server ? &poll_end : (timeout ? &end : NULL) );
             if (ret > 0)
             {
                 /* We must check this first! The server may set an event that
@@ -1124,7 +1255,12 @@ static NTSTATUS __esync_wait_objects( unsigned int count, const HANDLE *handles,
                 NtQuerySystemTime( &now );
             }
             else
+            {
+                NtQuerySystemTime( &now );
+                if (has_server && (!timeout || now.QuadPart < end))
+                    continue;
                 goto err;
+            }
         }
     }
     else
diff --git a/include/wine/server_protocol.h b/include/wine/server_protocol.h
index 73adac40946..858bcaeb050 100644
--- a/include/wine/server_protocol.h
+++ b/include/wine/server_protocol.h
@@ -5927,6 +5927,8 @@ struct get_esync_fd_reply
     struct reply_header __header;
     int          type;
     unsigned int shm_idx;
+    unsigned int access;
+    char __pad_20[4];
 };
 
 struct esync_msgwait_request
@@ -6873,6 +6875,6 @@ union generic_reply
     struct get_esync_apc_fd_reply get_esync_apc_fd_reply;
 };
 
-#define SERVER_PROTOCOL_VERSION 856
+#define SERVER_PROTOCOL_VERSION 857
 
 #endif /* __WINE_WINE_SERVER_PROTOCOL_H */
diff --git a/server/debugger.c b/server/debugger.c
index 0c01ec1c0d3..cf8f134066b 100644
--- a/server/debugger.c
+++ b/server/debugger.c
@@ -36,8 +36,12 @@
 #include "process.h"
 #include "thread.h"
 #include "request.h"
+#include "esync.h"
 
 enum debug_event_state { EVENT_QUEUED, EVENT_SENT, EVENT_DELAYED, EVENT_CONTINUED };
+struct debug_obj;
+struct debug_event;
+static struct debug_event *find_event_to_send( struct debug_obj *debug_obj );
 
 /* debug event */
 struct debug_event
@@ -71,6 +75,7 @@ struct debug_obj
     struct object        obj;         /* object header */
     struct list          event_queue; /* pending events queue */
     unsigned int         flags;       /* debug flags */
+    int                  esync_fd;    /* esync file descriptor */
 };
 
 
@@ -105,6 +110,7 @@ static const struct object_ops debug_event_ops =
 
 static void debug_obj_dump( struct object *obj, int verbose );
 static int debug_obj_signaled( struct object *obj, struct wait_queue_entry *entry );
+static int debug_obj_get_esync_fd( struct object *obj, enum esync_type *type );
 static void debug_obj_destroy( struct object *obj );
 
 static const struct object_ops debug_obj_ops =
@@ -115,7 +121,7 @@ static const struct object_ops debug_obj_ops =
     add_queue,                     /* add_queue */
     remove_queue,                  /* remove_queue */
     debug_obj_signaled,            /* signaled */
-    NULL,                          /* get_esync_fd */
+    debug_obj_get_esync_fd,        /* get_esync_fd */
     no_satisfied,                  /* satisfied */
     no_signal,                     /* signal */
     no_get_fd,                     /* get_fd */
@@ -243,6 +249,8 @@ static void unlink_event( struct debug_obj *debug_obj, struct debug_event *event
     list_remove( &event->entry );
     if (event->sender->process->debug_event == event) event->sender->process->debug_event = NULL;
     release_object( event );
+    if (do_esync() && !find_event_to_send( debug_obj ))
+        esync_clear( debug_obj->esync_fd );
 }
 
 /* link an event at the end of the queue */
@@ -332,6 +340,13 @@ static int debug_obj_signaled( struct object *obj, struct wait_queue_entry *entr
     return find_event_to_send( debug_obj ) != NULL;
 }
 
+static int debug_obj_get_esync_fd( struct object *obj, enum esync_type *type )
+{
+    struct debug_obj *debug_obj = (struct debug_obj *)obj;
+    *type = ESYNC_MANUAL_SERVER;
+    return debug_obj->esync_fd;
+}
+
 static void debug_obj_destroy( struct object *obj )
 {
     struct list *ptr;
@@ -344,6 +359,8 @@ static void debug_obj_destroy( struct object *obj )
     /* free all pending events */
     while ((ptr = list_head( &debug_obj->event_queue )))
         unlink_event( debug_obj, LIST_ENTRY( ptr, struct debug_event, entry ));
+
+    if (do_esync()) close( debug_obj->esync_fd );
 }
 
 struct debug_obj *get_debug_obj( struct process *process, obj_handle_t handle, unsigned int access )
@@ -362,7 +379,10 @@ static struct debug_obj *create_debug_obj( struct object *root, const struct uni
         if (get_error() != STATUS_OBJECT_NAME_EXISTS)
         {
             debug_obj->flags = flags;
+            debug_obj->esync_fd = -1;
             list_init( &debug_obj->event_queue );
+            if (do_esync())
+                debug_obj->esync_fd = esync_create_fd( 0, 0 );
         }
     }
     return debug_obj;
diff --git a/server/esync.c b/server/esync.c
index 62be734bdeb..656fcb1c821 100644
--- a/server/esync.c
+++ b/server/esync.c
@@ -68,68 +68,45 @@ static long pagesize;
 #include <alloca.h>
 #include <errno.h>
 static int shm_unlink(const char *name) {
-    size_t namelen;
     const char *env_tmpdir = getenv("TMPDIR");
     const char *default_tmpdir = "/data/data/com.windroid.emu/files/usr/tmp/";
-
+    const char *dir = env_tmpdir ? env_tmpdir : default_tmpdir;
     char fname[1024];
+    size_t pathlen;
 
-    if (env_tmpdir) {
-        size_t pathlen = strlen(env_tmpdir);
-        snprintf(fname, sizeof(fname), "%s%s", env_tmpdir, (env_tmpdir[pathlen - 1] == '/') ? "" : "/");
-    } else {
-        snprintf(fname, sizeof(fname), "%s", default_tmpdir);
-    }
-
-    /* Construct the filename.  */
     while (name[0] == '/') ++name;
-
     if (name[0] == '\0') {
-        /* The name "/" is not supported.  */
         errno = EINVAL;
         return -1;
     }
 
-    strcat(fname, name);
-
+    pathlen = strlen(dir);
+    snprintf(fname, sizeof(fname), "%s%s%s", dir, (pathlen > 0 && dir[pathlen - 1] == '/') ? "" : "/", name);
     return unlink(fname);
 }
 
 static int shm_open(const char *name, int oflag, mode_t mode) {
-    size_t namelen;
-    int fd;
     const char *env_tmpdir = getenv("TMPDIR");
     const char *default_tmpdir = "/data/data/com.windroid.emu/files/usr/tmp/";
-
+    const char *dir = env_tmpdir ? env_tmpdir : default_tmpdir;
     char fname[1024];
+    size_t pathlen;
+    int fd;
 
-    if (env_tmpdir) {
-        size_t pathlen = strlen(env_tmpdir);
-        snprintf(fname, sizeof(fname), "%s%s", env_tmpdir, (env_tmpdir[pathlen - 1] == '/') ? "" : "/");
-    } else {
-        snprintf(fname, sizeof(fname), "%s", default_tmpdir);
-    }
-
-    /* Construct the filename.  */
     while (name[0] == '/') ++name;
-
     if (name[0] == '\0') {
-        /* The name "/" is not supported.  */
         errno = EINVAL;
         return -1;
     }
 
-    strcat(fname, name);
+    pathlen = strlen(dir);
+    snprintf(fname, sizeof(fname), "%s%s%s", dir, (pathlen > 0 && dir[pathlen - 1] == '/') ? "" : "/", name);
 
     fd = open(fname, oflag, mode);
     if (fd != -1) {
-        /* We got a descriptor.  Now set the FD_CLOEXEC bit.  */
         int flags = fcntl(fd, F_GETFD, 0);
         flags |= FD_CLOEXEC;
-        flags = fcntl(fd, F_SETFD, flags);
-
-        if (flags == -1) {
-            /* Something went wrong.  We cannot return the descriptor.  */
+        if (fcntl(fd, F_SETFD, flags) == -1) {
             int save_errno = errno;
             close(fd);
             fd = -1;
@@ -636,6 +613,7 @@ DECL_HANDLER(get_esync_fd)
     {
         fd = obj->ops->get_esync_fd( obj, &type );
         reply->type = type;
+        reply->access = get_handle_access( current->process, req->handle );
         if (obj->ops == &esync_ops)
         {
             struct esync *esync = (struct esync *)obj;
diff --git a/server/named_pipe.c b/server/named_pipe.c
index fa74ff8244a..eb7cca73a71 100644
--- a/server/named_pipe.c
+++ b/server/named_pipe.c
@@ -293,7 +293,7 @@ static const struct object_ops named_pipe_device_file_ops =
     add_queue,                               /* add_queue */
     remove_queue,                            /* remove_queue */
     default_fd_signaled,                     /* signaled */
-    NULL,                                    /* get_esync_fd */
+    default_fd_get_esync_fd,                 /* get_esync_fd */
     no_satisfied,                            /* satisfied */
     no_signal,                               /* signal */
     named_pipe_device_file_get_fd,           /* get_fd */
@@ -344,7 +344,7 @@ static const struct object_ops named_pipe_dir_ops =
     add_queue,                               /* add_queue */
     remove_queue,                            /* remove_queue */
     default_fd_signaled,                     /* signaled */
-    NULL,                                    /* get_esync_fd */
+    default_fd_get_esync_fd,                 /* get_esync_fd */
     no_satisfied,                            /* satisfied */
     no_signal,                               /* signal */
     named_pipe_dir_get_fd,                   /* get_fd */
diff --git a/server/protocol.def b/server/protocol.def
index ffefbd20e1e..4506f75ec77 100644
--- a/server/protocol.def
+++ b/server/protocol.def
@@ -4112,6 +4112,7 @@ enum esync_type
 @REPLY
     int          type;
     unsigned int shm_idx;
+    unsigned int access;
 @END
 
 @REQ(esync_msgwait)
diff --git a/server/request_handlers.h b/server/request_handlers.h
index 5747dae4b7c..8f870495efb 100644
--- a/server/request_handlers.h
+++ b/server/request_handlers.h
@@ -2296,7 +2296,8 @@ C_ASSERT( offsetof(struct get_esync_fd_request, handle) == 12 );
 C_ASSERT( sizeof(struct get_esync_fd_request) == 16 );
 C_ASSERT( offsetof(struct get_esync_fd_reply, type) == 8 );
 C_ASSERT( offsetof(struct get_esync_fd_reply, shm_idx) == 12 );
-C_ASSERT( sizeof(struct get_esync_fd_reply) == 16 );
+C_ASSERT( offsetof(struct get_esync_fd_reply, access) == 16 );
+C_ASSERT( sizeof(struct get_esync_fd_reply) == 24 );
 C_ASSERT( offsetof(struct esync_msgwait_request, in_msgwait) == 12 );
 C_ASSERT( sizeof(struct esync_msgwait_request) == 16 );
 C_ASSERT( offsetof(struct set_keyboard_repeat_request, enable) == 12 );
diff --git a/server/request_trace.h b/server/request_trace.h
index ce23329a99b..de66ae83a18 100644
--- a/server/request_trace.h
+++ b/server/request_trace.h
@@ -3373,6 +3373,7 @@ static void dump_get_esync_fd_reply( const struct get_esync_fd_reply *req )
 {
     fprintf( stderr, " type=%d", req->type );
     fprintf( stderr, ", shm_idx=%08x", req->shm_idx );
+    fprintf( stderr, ", access=%08x", req->access );
 }
 
 static void dump_esync_msgwait_request( const struct esync_msgwait_request *req )
diff --git a/server/sock.c b/server/sock.c
index 2bb027a563c..af112b21e4f 100644
--- a/server/sock.c
+++ b/server/sock.c
@@ -471,7 +471,7 @@ static const struct object_ops sock_ops =
     add_queue,                    /* add_queue */
     remove_queue,                 /* remove_queue */
     default_fd_signaled,          /* signaled */
-    NULL,                         /* get_esync_fd */
+    default_fd_get_esync_fd,      /* get_esync_fd */
     no_satisfied,                 /* satisfied */
     no_signal,                    /* signal */
     sock_get_fd,                  /* get_fd */
diff --git a/server/timer.c b/server/timer.c
index b4ab6113211..cc46ebcc38b 100644
--- a/server/timer.c
+++ b/server/timer.c
@@ -177,6 +177,9 @@ static int cancel_timer( struct timer *timer )
         release_object( timer->thread );
         timer->thread = NULL;
     }
+    if (do_esync())
+        esync_clear( timer->esync_fd );
+    timer->signaled = 0;
     return signaled;
 }
 
@@ -188,11 +191,10 @@ static int set_timer( struct timer *timer, timeout_t expire, unsigned int period
     if (timer->manual)
     {
         period = 0;  /* period doesn't make any sense for a manual timer */
-        timer->signaled = 0;
-
-        if (do_esync())
-            esync_clear( timer->esync_fd );
     }
+    timer->signaled = 0;
+    if (do_esync())
+        esync_clear( timer->esync_fd );
     timer->when     = (expire <= 0) ? expire - monotonic_time : max( expire, current_time );
     timer->period   = period;
     timer->callback = callback;
